{
  "name": "kfolder",
  "version": "1.0.0",
  "type": "module",
  "description": "CLI que lista recursivamente todos os arquivos de um diretório em ordem alfabética.",
  "license": "MIT",
  "main": "dist/cli.js",
  "bin": {
    "kfolder": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts"
  },
  "keywords": ["cli", "typescript", "filesystem"],
  "author": "Seu Nome",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
